this is a binary search tree code from week 9 lecture notes
march 14, 
implements struct bst_node, wrapper for it bst, and few function that can operate on the 
tree, insert(D, x), contains(D,x), remove(D,x) etc.. 
